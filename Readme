介绍：
本程序是基于Python开发的Ollama的客户端，具有美观的操作界面，具有上下文记忆和切换模型的功能；

本程序需要Python环境，请自行配置；
本程序需要服务端，可以配置在本机或其他服务器；
具体安装请参阅Ollama官网：https://ollama.com/

包含的依赖：
sys requests json PyQt6

self.api_url = "http://localhost:11434/api/generate"
可以替换为本地其他服务器地址；

self.current_model = "qwen2:0.5b"
模型可以修改，此配置为默认模型；

model_names = ["qwen2:0.5b", "qwen2:1.5b", "qwen2:7b"]
可以在服务端键入： ollama list 查询已存在的模型；
